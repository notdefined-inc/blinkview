//! openfoto — a local-first photo organizer. Your folders are the database.

use anyhow::Result;

fn main() -> Result<()> {
    println!("openfoto {} — scaffolded, not yet implemented.", env!("CARGO_PKG_VERSION"));
    println!("See docs/CURRENT/STATUS.md and docs/SPECS/active/ for what is planned.");
    Ok(())
}
