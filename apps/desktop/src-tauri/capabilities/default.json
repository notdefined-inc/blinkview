{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Blinkview window. The UI-verification bridge is not here: it is a development-only plugin, and naming its permission would make a shipping build fail to compile.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-internal-toggle-devtools",
    "dialog:default",
    "core:window:allow-set-position"
  ]
}
