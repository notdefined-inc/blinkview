/target
**/*.rs.bk
.DS_Store
/models/*.onnx

# A library cache, if someone runs the CLI from the repo root — easy to do, since
# --library defaults to the current directory. Disposable by design; never committed.
.openfoto/
